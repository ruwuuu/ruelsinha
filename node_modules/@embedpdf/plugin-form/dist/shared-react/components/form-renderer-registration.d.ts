/**
 * Utility component that registers form field renderers once at app level.
 * Added via addUtility() so it mounts once, not per-page.
 */
export declare function FormRendererRegistration(): null;
