import { Rect } from '@embedpdf/models';
type $$ComponentProps = {
    data: unknown;
    bounds: Rect;
    scale: number;
};
declare const CheckboxPreview: import('svelte', { with: { "resolution-mode": "import" } }).Component<$$ComponentProps, {}, "">;
type CheckboxPreview = ReturnType<typeof CheckboxPreview>;
export default CheckboxPreview;
