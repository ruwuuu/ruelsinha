import { Rect } from '@embedpdf/models';
type $$ComponentProps = {
    data: unknown;
    bounds: Rect;
    scale: number;
};
declare const ListboxPreview: import('svelte', { with: { "resolution-mode": "import" } }).Component<$$ComponentProps, {}, "">;
type ListboxPreview = ReturnType<typeof ListboxPreview>;
export default ListboxPreview;
