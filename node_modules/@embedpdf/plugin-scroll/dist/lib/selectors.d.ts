import { ScrollerLayout, ScrollDocumentState } from './types';
export declare const getScrollerLayout: (documentState: ScrollDocumentState, scale: number, elevatedPages?: Set<number>) => ScrollerLayout;
